/**
 * Admin Dashboard Translations
 * 
 * This file exports translation resources for the admin dashboard.
 * Currently, no custom translations are configured.
 * 
 * To add translations:
 * 1. Create JSON files in ./json/ directory (e.g., en.json, de.json)
 * 2. Import and export them here
 * 
 * Example:
 * ```typescript
 * import en from "./json/en.json" with { type: "json" };
 * 
 * export default {
 *   en: {
 *     translation: en,
 *   },
 * };
 * ```
 */
export default {}